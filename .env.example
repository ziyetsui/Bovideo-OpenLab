# Payload Node write plane
PAYLOAD_SECRET=
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/postgres
DB_POOL_MAX=10

# Local tests only. Production schema changes run through explicit migrations.
PAYLOAD_DB_PUSH=false
